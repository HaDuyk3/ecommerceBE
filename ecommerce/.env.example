# Optional: Spring Boot does not load .env automatically.
# Set these in your IDE run config, shell, or use application-local.properties instead.

DB_PASSWORD=your_mysql_password
# Generate with: openssl rand -base64 64
JWT_SECRET=your_jwt_secret_here
